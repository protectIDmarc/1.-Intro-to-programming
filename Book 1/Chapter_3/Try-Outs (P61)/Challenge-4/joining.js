// Create an array of numbers
var numbers = [3, 2, 1];

// Join all elements in the array into a single string
// The string " is bigger than " will be placed between each number
var result = numbers.join(" is bigger than ");

// Output the final string to the console
console.log(result);